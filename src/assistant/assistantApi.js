import{buildAssistantContext}from'./assistantContext.js'
export async function askBrevityAssistant({messages,member,activeView,activePillar,pageLabel,storage=localStorage}){const latest=messages.at(-1)?.content||'';const context=buildAssistantContext({storage,member,activeView,activePillar,pageLabel,query:latest});const response=await fetch('/.netlify/functions/brevity-assistant',{method:'POST',credentials:'include',headers:{'content-type':'application/json'},body:JSON.stringify({messages:messages.slice(-16),page:{activeView,activePillar,pageLabel},context})});const payload=await response.json().catch(()=>({}));if(!response.ok)throw new Error(payload.error||'Brevity Assistant could not answer right now.');return payload}
