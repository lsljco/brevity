export function fourWeekCheckDue({programStart='2026-09-16',date,lastCheckDate=''}){const start=new Date(`${programStart}T00:00:00Z`),current=new Date(`${date}T00:00:00Z`);if(Number.isNaN(start)||Number.isNaN(current)||current<start)return false;const days=Math.floor((current-start)/86400000);if(days<28)return false;if(!lastCheckDate)return true;return Math.floor((current-new Date(`${lastCheckDate}T00:00:00Z`))/86400000)>=28}
