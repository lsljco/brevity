import {getStore} from '@netlify/blobs'
import householdAuth from './household-auth.js'
const {readSession}=householdAuth
const HOUSEHOLD_ID=process.env.BREVITY_HOUSEHOLD_ID||'lslj-family',STORE_NAME='brevity-sermon-slides'
const store=()=>getStore({name:STORE_NAME,consistency:'strong',siteID:process.env.NETLIFY_SITE_ID,token:process.env.NETLIFY_TOKEN})
const statusKey=id=>`${HOUSEHOLD_ID}/slides/${id}/status`,fileKey=id=>`${HOUSEHOLD_ID}/slides/${id}/deck.pptx`
export default async function handler(request){const session=await readSession({headers:{cookie:request.headers.get('cookie')||''}}).catch(()=>null);if(!session)return new Response(JSON.stringify({error:'Sign in to access sermon slides.'}),{status:401,headers:{'content-type':'application/json'}});const url=new URL(request.url),id=String(url.searchParams.get('id')||'').replace(/[^a-zA-Z0-9_-]/g,'').slice(0,120);if(!id)return new Response(JSON.stringify({error:'A slide-deck id is required.'}),{status:400,headers:{'content-type':'application/json'}});const dataStore=store();if(url.searchParams.get('download')){const [bytes,status]=await Promise.all([dataStore.get(fileKey(id),{type:'arrayBuffer'}).catch(()=>null),dataStore.get(statusKey(id),{type:'json'}).catch(()=>null)]);if(!bytes)return new Response('Slide deck not found.',{status:404});const filename=String(status?.fileName||`${id}-sermon-slides.pptx`).replace(/["\r\n]/g,'');return new Response(bytes,{headers:{'content-type':'application/vnd.openxmlformats-officedocument.presentationml.presentation','content-disposition':`attachment; filename="${filename}"`,'cache-control':'private, no-store'}})}const status=await dataStore.get(statusKey(id),{type:'json'}).catch(()=>null);return new Response(JSON.stringify(status||{state:'not-started'}),{headers:{'content-type':'application/json','cache-control':'no-store'}})}
export const config={path:'/.netlify/functions/sermon-slides'}
