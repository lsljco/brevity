import {getStore} from '@netlify/blobs'
import householdAuth from './household-auth.js'
import {buildSermonSlides,sermonSlidesFileName} from '../lib/sermon-slides.mjs'
import {getOneDriveConnection,publishSermonSlides} from '../lib/onedrive.mjs'
import {publishVisualTargets} from '../lib/onedrive-targets.mjs'
import {sermonGuideBaseName} from '../lib/sermon-times-documents.mjs'
const {readSession}=householdAuth
const HOUSEHOLD_ID=process.env.BREVITY_HOUSEHOLD_ID||'lslj-family',STORE_NAME='brevity-sermon-slides'
const store=()=>getStore({name:STORE_NAME,consistency:'strong',siteID:process.env.NETLIFY_SITE_ID,token:process.env.NETLIFY_TOKEN})
const statusKey=id=>`${HOUSEHOLD_ID}/slides/${id}/status`,fileKey=id=>`${HOUSEHOLD_ID}/slides/${id}/deck.pptx`,assetKey=(id,index)=>`${HOUSEHOLD_ID}/slides/${id}/assets/${String(index).padStart(2,'0')}.png`
export default async function handler(request){const session=await readSession({headers:{cookie:request.headers.get('cookie')||''}}).catch(()=>null);if(!session)return new Response(JSON.stringify({error:'Sign in to create sermon slides.'}),{status:401,headers:{'content-type':'application/json'}});const body=await request.json().catch(()=>({}));const id=String(body.id||'').replace(/[^a-zA-Z0-9_-]/g,'').slice(0,120);if(!id||!body.notes)return new Response(JSON.stringify({error:'Sermon notes are required.'}),{status:400,headers:{'content-type':'application/json'}});const dataStore=store(),fileName=sermonSlidesFileName(body.notes,body.source||{});await dataStore.setJSON(statusKey(id),{state:'generating',completed:0,total:0,fileName,startedAt:new Date().toISOString()});try{const result=await buildSermonSlides(body.notes,body.source||{},progress=>dataStore.setJSON(statusKey(id),{state:'generating',...progress,fileName,updatedAt:new Date().toISOString()}));await dataStore.set(fileKey(id),result.buffer);await Promise.all(result.assets.map(asset=>dataStore.set(assetKey(id,asset.index),asset.buffer)));let oneDrive={state:'not-connected'},visuals={state:'not-connected'};try{const baseName=sermonGuideBaseName(body.notes.documentTitle||body.source?.title||'Sermon',body.source?.sermonDate||body.notes.sermonDate);visuals=await publishVisualTargets({assets:result.assets,baseName})}catch(error){console.error('[sermon-visuals onedrive]',error);visuals={state:'error',error:error.message||'OneDrive image publishing failed.'}}if(await getOneDriveConnection()){try{oneDrive=await publishSermonSlides({pptx:result.buffer,fileName})}catch(error){console.error('[sermon-slides onedrive]',error);oneDrive={state:'error',error:error.message||'OneDrive slide publishing failed.'}}}await dataStore.setJSON(statusKey(id),{state:'ready',slideCount:result.slideCount,completed:result.slideCount,total:result.slideCount,fileName,oneDrive,visuals,visualCount:result.assets.length,updatedAt:new Date().toISOString(),download:`/.netlify/functions/sermon-slides?id=${encodeURIComponent(id)}&download=1`})}catch(error){console.error('[sermon-slides-background]',error);await dataStore.setJSON(statusKey(id),{state:'error',error:error.message||'Brevity could not create the sermon slides.',fileName,updatedAt:new Date().toISOString()})}return new Response(JSON.stringify({accepted:true}),{status:202,headers:{'content-type':'application/json'}})}
export const config={background:true,path:'/.netlify/functions/sermon-slides-background'}
